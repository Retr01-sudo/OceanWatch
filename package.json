{
  "name": "oceanguard-mvp",
  "version": "1.0.0",
  "description": "OceanGuard MVP - A web-based platform for citizens to report oceanic hazards",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd packages/backend-api && npm run dev",
    "dev:frontend": "cd packages/web-app && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd packages/backend-api && npm run build",
    "build:frontend": "cd packages/web-app && npm run build",
    "install:all": "npm install && cd packages/backend-api && npm install && cd ../web-app && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}

